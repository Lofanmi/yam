// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("yam", "for yam support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("yam", "enable yam support", "no");

if (PHP_YAM != "no") {
	EXTENSION("yam", "yam.c");
}

